/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetPaymentQrQueryResponse, GetPaymentQrPathParams } from "../../types/appointmentsController/GetPaymentQr.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getPaymentQrQueryKey = (id: GetPaymentQrPathParams["id"]) => [{ url: '/api/appointments/:id/payment-qr', params: {id:id} }] as const

export type GetPaymentQrQueryKey = ReturnType<typeof getPaymentQrQueryKey>

/**
 * @description Generate VietQR code for a pending-payment appointment. Only available when status is PENDING_PAYMENT.
 * @summary Get payment QR code
 * {@link /api/appointments/:id/payment-qr}
 */
export async function getPaymentQr(id: GetPaymentQrPathParams["id"], config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetPaymentQrQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : `/api/appointments/${id}/payment-qr`, ... requestConfig })  
  return res.data
}

export function getPaymentQrQueryOptions(id: GetPaymentQrPathParams["id"], config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const queryKey = getPaymentQrQueryKey(id)
  return queryOptions<GetPaymentQrQueryResponse, ResponseErrorConfig<Error>, GetPaymentQrQueryResponse, typeof queryKey>({
   enabled: !!(id),
   queryKey,
   queryFn: async ({ signal }) => {
      config.signal = signal
      return getPaymentQr(id, config)
   },
  })
}

/**
 * @description Generate VietQR code for a pending-payment appointment. Only available when status is PENDING_PAYMENT.
 * @summary Get payment QR code
 * {@link /api/appointments/:id/payment-qr}
 */
export function useGetPaymentQr<TData = GetPaymentQrQueryResponse, TQueryData = GetPaymentQrQueryResponse, TQueryKey extends QueryKey = GetPaymentQrQueryKey>(id: GetPaymentQrPathParams["id"], options: 
{
  query?: Partial<QueryObserverOptions<GetPaymentQrQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<RequestConfig> & { client?: typeof fetch }
}
 = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getPaymentQrQueryKey(id)

  const query = useQuery({
   ...getPaymentQrQueryOptions(id, config),
   queryKey,
   ...queryOptions
  } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}