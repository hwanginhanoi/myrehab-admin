/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetClinicScheduleQueryResponse } from "../../types/appointmentsController/GetClinicSchedule.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getClinicScheduleQueryKey = () => [{ url: '/api/appointments/clinic-schedule' }] as const

export type GetClinicScheduleQueryKey = ReturnType<typeof getClinicScheduleQueryKey>

/**
 * @description View clinic operating hours for all days
 * @summary Get clinic schedule
 * {@link /api/appointments/clinic-schedule}
 */
export async function getClinicSchedule(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetClinicScheduleQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : `/api/appointments/clinic-schedule`, ... requestConfig })  
  return res.data
}

export function getClinicScheduleQueryOptions(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const queryKey = getClinicScheduleQueryKey()
  return queryOptions<GetClinicScheduleQueryResponse, ResponseErrorConfig<Error>, GetClinicScheduleQueryResponse, typeof queryKey>({
 
   queryKey,
   queryFn: async ({ signal }) => {
      config.signal = signal
      return getClinicSchedule(config)
   },
  })
}

/**
 * @description View clinic operating hours for all days
 * @summary Get clinic schedule
 * {@link /api/appointments/clinic-schedule}
 */
export function useGetClinicSchedule<TData = GetClinicScheduleQueryResponse, TQueryData = GetClinicScheduleQueryResponse, TQueryKey extends QueryKey = GetClinicScheduleQueryKey>(options: 
{
  query?: Partial<QueryObserverOptions<GetClinicScheduleQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<RequestConfig> & { client?: typeof fetch }
}
 = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getClinicScheduleQueryKey()

  const query = useQuery({
   ...getClinicScheduleQueryOptions(config),
   queryKey,
   ...queryOptions
  } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}