/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type {
  GetPatientAppointmentsForStaffQueryResponse,
  GetPatientAppointmentsForStaffPathParams,
  GetPatientAppointmentsForStaffQueryParams,
} from '../../types/appointmentsController/GetPatientAppointmentsForStaff.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query'

export const getPatientAppointmentsForStaffSuspenseQueryKey = (
  userId: GetPatientAppointmentsForStaffPathParams['userId'],
  params: GetPatientAppointmentsForStaffQueryParams
) =>
  [
    {
      url: '/api/appointments/staff/patient/:userId',
      params: { userId: userId },
    },
    ...(params ? [params] : []),
  ] as const

export type GetPatientAppointmentsForStaffSuspenseQueryKey = ReturnType<
  typeof getPatientAppointmentsForStaffSuspenseQueryKey
>

/**
 * @description Any staff member views appointments for a specific patient
 * @summary Get patient appointments (staff)
 * {@link /api/appointments/staff/patient/:userId}
 */
export async function getPatientAppointmentsForStaffSuspense(
  userId: GetPatientAppointmentsForStaffPathParams['userId'],
  params: GetPatientAppointmentsForStaffQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetPatientAppointmentsForStaffQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: `/api/appointments/staff/patient/${userId}`,
    params,
    ...requestConfig,
  })
  return res.data
}

export function getPatientAppointmentsForStaffSuspenseQueryOptions(
  userId: GetPatientAppointmentsForStaffPathParams['userId'],
  params: GetPatientAppointmentsForStaffQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getPatientAppointmentsForStaffSuspenseQueryKey(
    userId,
    params
  )
  return queryOptions<
    GetPatientAppointmentsForStaffQueryResponse,
    ResponseErrorConfig<Error>,
    GetPatientAppointmentsForStaffQueryResponse,
    typeof queryKey
  >({
    enabled: !!(userId && params),
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getPatientAppointmentsForStaffSuspense(userId, params, config)
    },
  })
}

/**
 * @description Any staff member views appointments for a specific patient
 * @summary Get patient appointments (staff)
 * {@link /api/appointments/staff/patient/:userId}
 */
export function useGetPatientAppointmentsForStaffSuspense<
  TData = GetPatientAppointmentsForStaffQueryResponse,
  TQueryKey extends QueryKey = GetPatientAppointmentsForStaffSuspenseQueryKey,
>(
  userId: GetPatientAppointmentsForStaffPathParams['userId'],
  params: GetPatientAppointmentsForStaffQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetPatientAppointmentsForStaffQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey =
    queryOptions?.queryKey ??
    getPatientAppointmentsForStaffSuspenseQueryKey(userId, params)

  const query = useSuspenseQuery(
    {
      ...getPatientAppointmentsForStaffSuspenseQueryOptions(
        userId,
        params,
        config
      ),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
