/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type {
  GetPatientAppointmentsForStaffQueryResponse,
  GetPatientAppointmentsForStaffPathParams,
  GetPatientAppointmentsForStaffQueryParams,
} from '../../types/appointmentsController/GetPatientAppointmentsForStaff.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getPatientAppointmentsForStaffQueryKey = (
  userId: GetPatientAppointmentsForStaffPathParams['userId'],
  params: GetPatientAppointmentsForStaffQueryParams
) =>
  [
    {
      url: '/api/appointments/staff/patient/:userId',
      params: { userId: userId },
    },
    ...(params ? [params] : []),
  ] as const

export type GetPatientAppointmentsForStaffQueryKey = ReturnType<
  typeof getPatientAppointmentsForStaffQueryKey
>

/**
 * @description Any staff member views appointments for a specific patient
 * @summary Get patient appointments (staff)
 * {@link /api/appointments/staff/patient/:userId}
 */
export async function getPatientAppointmentsForStaff(
  userId: GetPatientAppointmentsForStaffPathParams['userId'],
  params: GetPatientAppointmentsForStaffQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetPatientAppointmentsForStaffQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: `/api/appointments/staff/patient/${userId}`,
    params,
    ...requestConfig,
  })
  return res.data
}

export function getPatientAppointmentsForStaffQueryOptions(
  userId: GetPatientAppointmentsForStaffPathParams['userId'],
  params: GetPatientAppointmentsForStaffQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getPatientAppointmentsForStaffQueryKey(userId, params)
  return queryOptions<
    GetPatientAppointmentsForStaffQueryResponse,
    ResponseErrorConfig<Error>,
    GetPatientAppointmentsForStaffQueryResponse,
    typeof queryKey
  >({
    enabled: !!(userId && params),
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getPatientAppointmentsForStaff(userId, params, config)
    },
  })
}

/**
 * @description Any staff member views appointments for a specific patient
 * @summary Get patient appointments (staff)
 * {@link /api/appointments/staff/patient/:userId}
 */
export function useGetPatientAppointmentsForStaff<
  TData = GetPatientAppointmentsForStaffQueryResponse,
  TQueryData = GetPatientAppointmentsForStaffQueryResponse,
  TQueryKey extends QueryKey = GetPatientAppointmentsForStaffQueryKey,
>(
  userId: GetPatientAppointmentsForStaffPathParams['userId'],
  params: GetPatientAppointmentsForStaffQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetPatientAppointmentsForStaffQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey =
    queryOptions?.queryKey ??
    getPatientAppointmentsForStaffQueryKey(userId, params)

  const query = useQuery(
    {
      ...getPatientAppointmentsForStaffQueryOptions(userId, params, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
