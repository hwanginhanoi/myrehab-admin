/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetSttStatusQueryResponse, GetSttStatusPathParams } from "../../types/appointmentsController/GetSttStatus.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getSttStatusQueryKey = (id: GetSttStatusPathParams["id"]) => [{ url: '/api/appointments/:id/stt/status', params: {id:id} }] as const

export type GetSttStatusQueryKey = ReturnType<typeof getSttStatusQueryKey>

/**
 * @description Query the status of a running STT agent for an appointment
 * @summary Get STT status
 * {@link /api/appointments/:id/stt/status}
 */
export async function getSttStatus(id: GetSttStatusPathParams["id"], config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetSttStatusQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : `/api/appointments/${id}/stt/status`, ... requestConfig })  
  return res.data
}

export function getSttStatusQueryOptions(id: GetSttStatusPathParams["id"], config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const queryKey = getSttStatusQueryKey(id)
  return queryOptions<GetSttStatusQueryResponse, ResponseErrorConfig<Error>, GetSttStatusQueryResponse, typeof queryKey>({
   enabled: !!(id),
   queryKey,
   queryFn: async ({ signal }) => {
      config.signal = signal
      return getSttStatus(id, config)
   },
  })
}

/**
 * @description Query the status of a running STT agent for an appointment
 * @summary Get STT status
 * {@link /api/appointments/:id/stt/status}
 */
export function useGetSttStatus<TData = GetSttStatusQueryResponse, TQueryData = GetSttStatusQueryResponse, TQueryKey extends QueryKey = GetSttStatusQueryKey>(id: GetSttStatusPathParams["id"], options: 
{
  query?: Partial<QueryObserverOptions<GetSttStatusQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<RequestConfig> & { client?: typeof fetch }
}
 = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getSttStatusQueryKey(id)

  const query = useQuery({
   ...getSttStatusQueryOptions(id, config),
   queryKey,
   ...queryOptions
  } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}