/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type { GetPublicDoctorsQueryResponse } from '../../types/appointmentsController/GetPublicDoctors.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getPublicDoctorsQueryKey = () =>
  [{ url: '/api/appointments/doctors' }] as const

export type GetPublicDoctorsQueryKey = ReturnType<
  typeof getPublicDoctorsQueryKey
>

/**
 * @description Returns all public and enabled doctors. Accessible without authentication for guest browsing.
 * @summary List available doctors
 * {@link /api/appointments/doctors}
 */
export async function getPublicDoctors(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetPublicDoctorsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: 'GET', url: `/api/appointments/doctors`, ...requestConfig })
  return res.data
}

export function getPublicDoctorsQueryOptions(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getPublicDoctorsQueryKey()
  return queryOptions<
    GetPublicDoctorsQueryResponse,
    ResponseErrorConfig<Error>,
    GetPublicDoctorsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getPublicDoctors(config)
    },
  })
}

/**
 * @description Returns all public and enabled doctors. Accessible without authentication for guest browsing.
 * @summary List available doctors
 * {@link /api/appointments/doctors}
 */
export function useGetPublicDoctors<
  TData = GetPublicDoctorsQueryResponse,
  TQueryData = GetPublicDoctorsQueryResponse,
  TQueryKey extends QueryKey = GetPublicDoctorsQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetPublicDoctorsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getPublicDoctorsQueryKey()

  const query = useQuery(
    {
      ...getPublicDoctorsQueryOptions(config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
