/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type {
  SearchMyPatientsQueryResponse,
  SearchMyPatientsQueryParams,
} from '../../types/doctorController/SearchMyPatients.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query'

export const searchMyPatientsSuspenseQueryKey = (
  params?: SearchMyPatientsQueryParams
) =>
  [
    { url: '/api/doctors/patients/search' },
    ...(params ? [params] : []),
  ] as const

export type SearchMyPatientsSuspenseQueryKey = ReturnType<
  typeof searchMyPatientsSuspenseQueryKey
>

/**
 * @description Returns up to 5 closest matching patients assigned to the authenticated doctor, based on name or phone number. Used for the patient search filter dropdown.
 * @summary Search my patients (top 5 results)
 * {@link /api/doctors/patients/search}
 */
export async function searchMyPatientsSuspense(
  params?: SearchMyPatientsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    SearchMyPatientsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: `/api/doctors/patients/search`,
    params,
    ...requestConfig,
  })
  return res.data
}

export function searchMyPatientsSuspenseQueryOptions(
  params?: SearchMyPatientsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = searchMyPatientsSuspenseQueryKey(params)
  return queryOptions<
    SearchMyPatientsQueryResponse,
    ResponseErrorConfig<Error>,
    SearchMyPatientsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return searchMyPatientsSuspense(params, config)
    },
  })
}

/**
 * @description Returns up to 5 closest matching patients assigned to the authenticated doctor, based on name or phone number. Used for the patient search filter dropdown.
 * @summary Search my patients (top 5 results)
 * {@link /api/doctors/patients/search}
 */
export function useSearchMyPatientsSuspense<
  TData = SearchMyPatientsQueryResponse,
  TQueryKey extends QueryKey = SearchMyPatientsSuspenseQueryKey,
>(
  params?: SearchMyPatientsQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        SearchMyPatientsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey =
    queryOptions?.queryKey ?? searchMyPatientsSuspenseQueryKey(params)

  const query = useSuspenseQuery(
    {
      ...searchMyPatientsSuspenseQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
