/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type {
  GetVideoUrlQueryResponse,
  GetVideoUrlPathParams,
} from '../../types/exercisesController/GetVideoUrl.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query'

export const getVideoUrlSuspenseQueryKey = (id: GetVideoUrlPathParams['id']) =>
  [{ url: '/api/exercises/:id/video-url', params: { id: id } }] as const

export type GetVideoUrlSuspenseQueryKey = ReturnType<
  typeof getVideoUrlSuspenseQueryKey
>

/**
 * @description Generate a presigned URL to stream the exercise video. Access is verified by business logic.
 * @summary Get video URL
 * {@link /api/exercises/:id/video-url}
 */
export async function getVideoUrlSuspense(
  id: GetVideoUrlPathParams['id'],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetVideoUrlQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: 'GET', url: `/api/exercises/${id}/video-url`, ...requestConfig })
  return res.data
}

export function getVideoUrlSuspenseQueryOptions(
  id: GetVideoUrlPathParams['id'],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getVideoUrlSuspenseQueryKey(id)
  return queryOptions<
    GetVideoUrlQueryResponse,
    ResponseErrorConfig<Error>,
    GetVideoUrlQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getVideoUrlSuspense(id, config)
    },
  })
}

/**
 * @description Generate a presigned URL to stream the exercise video. Access is verified by business logic.
 * @summary Get video URL
 * {@link /api/exercises/:id/video-url}
 */
export function useGetVideoUrlSuspense<
  TData = GetVideoUrlQueryResponse,
  TQueryKey extends QueryKey = GetVideoUrlSuspenseQueryKey,
>(
  id: GetVideoUrlPathParams['id'],
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetVideoUrlQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getVideoUrlSuspenseQueryKey(id)

  const query = useSuspenseQuery(
    {
      ...getVideoUrlSuspenseQueryOptions(id, config),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
