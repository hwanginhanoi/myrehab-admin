/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
  GetVideoUrlQueryResponse,
  GetVideoUrlPathParams,
} from "../../types/exercisesController/GetVideoUrl.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getVideoUrlQueryKey = (id: GetVideoUrlPathParams["id"]) =>
  [{ url: "/api/exercises/:id/video-url", params: { id: id } }] as const;

export type GetVideoUrlQueryKey = ReturnType<typeof getVideoUrlQueryKey>;

/**
 * @description Generate a presigned URL to stream the exercise video. Access is verified by business logic.
 * @summary Get video URL
 * {@link /api/exercises/:id/video-url}
 */
export async function getVideoUrl(
  id: GetVideoUrlPathParams["id"],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetVideoUrlQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: "GET", url: `/api/exercises/${id}/video-url`, ...requestConfig });
  return res.data;
}

export function getVideoUrlQueryOptions(
  id: GetVideoUrlPathParams["id"],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getVideoUrlQueryKey(id);
  return queryOptions<
    GetVideoUrlQueryResponse,
    ResponseErrorConfig<Error>,
    GetVideoUrlQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getVideoUrl(id, config);
    },
  });
}

/**
 * @description Generate a presigned URL to stream the exercise video. Access is verified by business logic.
 * @summary Get video URL
 * {@link /api/exercises/:id/video-url}
 */
export function useGetVideoUrl<
  TData = GetVideoUrlQueryResponse,
  TQueryData = GetVideoUrlQueryResponse,
  TQueryKey extends QueryKey = GetVideoUrlQueryKey,
>(
  id: GetVideoUrlPathParams["id"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetVideoUrlQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey = queryOptions?.queryKey ?? getVideoUrlQueryKey(id);

  const query = useQuery(
    {
      ...getVideoUrlQueryOptions(id, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
