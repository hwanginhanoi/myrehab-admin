/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type { GetActiveBannersQueryResponse } from '../../types/bannerManagementController/GetActiveBanners.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getActiveBannersQueryKey = () => [{ url: '/api/banners' }] as const

export type GetActiveBannersQueryKey = ReturnType<
  typeof getActiveBannersQueryKey
>

/**
 * @description Returns all active banners ordered by display order. Public endpoint.
 * @summary Get active banners
 * {@link /api/banners}
 */
export async function getActiveBanners(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetActiveBannersQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: 'GET', url: `/api/banners`, ...requestConfig })
  return res.data
}

export function getActiveBannersQueryOptions(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getActiveBannersQueryKey()
  return queryOptions<
    GetActiveBannersQueryResponse,
    ResponseErrorConfig<Error>,
    GetActiveBannersQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getActiveBanners(config)
    },
  })
}

/**
 * @description Returns all active banners ordered by display order. Public endpoint.
 * @summary Get active banners
 * {@link /api/banners}
 */
export function useGetActiveBanners<
  TData = GetActiveBannersQueryResponse,
  TQueryData = GetActiveBannersQueryResponse,
  TQueryKey extends QueryKey = GetActiveBannersQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetActiveBannersQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getActiveBannersQueryKey()

  const query = useQuery(
    {
      ...getActiveBannersQueryOptions(config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
