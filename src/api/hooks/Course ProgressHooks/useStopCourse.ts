/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type {
  StopCourseMutationResponse,
  StopCoursePathParams,
} from '../../types/courseProgressController/StopCourse.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  UseMutationOptions,
  UseMutationResult,
  QueryClient,
} from '@tanstack/react-query'
import { mutationOptions, useMutation } from '@tanstack/react-query'

export const stopCourseMutationKey = () =>
  [{ url: '/api/course-progress/courses/:courseId/stop' }] as const

export type StopCourseMutationKey = ReturnType<typeof stopCourseMutationKey>

/**
 * @description Stop a course in progress and delete all progress entirely. Exercise progress and feedback are cascade-deleted. After stopping, the user can start a different course.
 * @summary Stop/abandon a course
 * {@link /api/course-progress/courses/:courseId/stop}
 */
export async function stopCourse(
  courseId: StopCoursePathParams['courseId'],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    StopCourseMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'POST',
    url: `/api/course-progress/courses/${courseId}/stop`,
    ...requestConfig,
  })
  return res.data
}

export function stopCourseMutationOptions(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const mutationKey = stopCourseMutationKey()
  return mutationOptions<
    StopCourseMutationResponse,
    ResponseErrorConfig<Error>,
    { courseId: StopCoursePathParams['courseId'] },
    typeof mutationKey
  >({
    mutationKey,
    mutationFn: async ({ courseId }) => {
      return stopCourse(courseId, config)
    },
  })
}

/**
 * @description Stop a course in progress and delete all progress entirely. Exercise progress and feedback are cascade-deleted. After stopping, the user can start a different course.
 * @summary Stop/abandon a course
 * {@link /api/course-progress/courses/:courseId/stop}
 */
export function useStopCourse<TContext>(
  options: {
    mutation?: UseMutationOptions<
      StopCourseMutationResponse,
      ResponseErrorConfig<Error>,
      { courseId: StopCoursePathParams['courseId'] },
      TContext
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { mutation = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...mutationOptions } = mutation
  const mutationKey = mutationOptions.mutationKey ?? stopCourseMutationKey()

  const baseOptions = stopCourseMutationOptions(config) as UseMutationOptions<
    StopCourseMutationResponse,
    ResponseErrorConfig<Error>,
    { courseId: StopCoursePathParams['courseId'] },
    TContext
  >

  return useMutation<
    StopCourseMutationResponse,
    ResponseErrorConfig<Error>,
    { courseId: StopCoursePathParams['courseId'] },
    TContext
  >(
    {
      ...baseOptions,
      mutationKey,
      ...mutationOptions,
    },
    queryClient
  ) as UseMutationResult<
    StopCourseMutationResponse,
    ResponseErrorConfig<Error>,
    { courseId: StopCoursePathParams['courseId'] },
    TContext
  >
}
