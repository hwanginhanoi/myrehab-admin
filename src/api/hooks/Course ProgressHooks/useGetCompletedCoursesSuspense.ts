/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type {
  GetCompletedCoursesQueryResponse,
  GetCompletedCoursesQueryParams,
} from '../../types/courseProgressController/GetCompletedCourses.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query'

export const getCompletedCoursesSuspenseQueryKey = (
  params: GetCompletedCoursesQueryParams
) =>
  [
    { url: '/api/course-progress/completed' },
    ...(params ? [params] : []),
  ] as const

export type GetCompletedCoursesSuspenseQueryKey = ReturnType<
  typeof getCompletedCoursesSuspenseQueryKey
>

/**
 * @description Retrieve all successfully completed courses (archived with COMPLETED reason). Excludes courses that were restarted or expired. Shows user's achievements and finished rehabilitation programs.
 * @summary Get my completed courses
 * {@link /api/course-progress/completed}
 */
export async function getCompletedCoursesSuspense(
  params: GetCompletedCoursesQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetCompletedCoursesQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: `/api/course-progress/completed`,
    params,
    ...requestConfig,
  })
  return res.data
}

export function getCompletedCoursesSuspenseQueryOptions(
  params: GetCompletedCoursesQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getCompletedCoursesSuspenseQueryKey(params)
  return queryOptions<
    GetCompletedCoursesQueryResponse,
    ResponseErrorConfig<Error>,
    GetCompletedCoursesQueryResponse,
    typeof queryKey
  >({
    enabled: !!params,
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getCompletedCoursesSuspense(params, config)
    },
  })
}

/**
 * @description Retrieve all successfully completed courses (archived with COMPLETED reason). Excludes courses that were restarted or expired. Shows user's achievements and finished rehabilitation programs.
 * @summary Get my completed courses
 * {@link /api/course-progress/completed}
 */
export function useGetCompletedCoursesSuspense<
  TData = GetCompletedCoursesQueryResponse,
  TQueryKey extends QueryKey = GetCompletedCoursesSuspenseQueryKey,
>(
  params: GetCompletedCoursesQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetCompletedCoursesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey =
    queryOptions?.queryKey ?? getCompletedCoursesSuspenseQueryKey(params)

  const query = useSuspenseQuery(
    {
      ...getCompletedCoursesSuspenseQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
