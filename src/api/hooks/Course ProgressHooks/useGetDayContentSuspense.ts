/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
  GetDayContentQueryResponse,
  GetDayContentPathParams,
} from "../../types/courseProgressController/GetDayContent.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getDayContentSuspenseQueryKey = (
  courseId: GetDayContentPathParams["courseId"],
  dayNumber: GetDayContentPathParams["dayNumber"],
) =>
  [
    {
      url: "/api/course-progress/courses/:courseId/days/:dayNumber",
      params: { courseId: courseId, dayNumber: dayNumber },
    },
  ] as const;

export type GetDayContentSuspenseQueryKey = ReturnType<
  typeof getDayContentSuspenseQueryKey
>;

/**
 * @description Retrieve all exercises for a specific day with completion status. Returns 423 LOCKED if day is not yet unlocked. User must complete Day N to access Day N+1.
 * @summary Get day's exercises
 * {@link /api/course-progress/courses/:courseId/days/:dayNumber}
 */
export async function getDayContentSuspense(
  courseId: GetDayContentPathParams["courseId"],
  dayNumber: GetDayContentPathParams["dayNumber"],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetDayContentQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: `/api/course-progress/courses/${courseId}/days/${dayNumber}`,
    ...requestConfig,
  });
  return res.data;
}

export function getDayContentSuspenseQueryOptions(
  courseId: GetDayContentPathParams["courseId"],
  dayNumber: GetDayContentPathParams["dayNumber"],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getDayContentSuspenseQueryKey(courseId, dayNumber);
  return queryOptions<
    GetDayContentQueryResponse,
    ResponseErrorConfig<Error>,
    GetDayContentQueryResponse,
    typeof queryKey
  >({
    enabled: !!(courseId && dayNumber),
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getDayContentSuspense(courseId, dayNumber, config);
    },
  });
}

/**
 * @description Retrieve all exercises for a specific day with completion status. Returns 423 LOCKED if day is not yet unlocked. User must complete Day N to access Day N+1.
 * @summary Get day's exercises
 * {@link /api/course-progress/courses/:courseId/days/:dayNumber}
 */
export function useGetDayContentSuspense<
  TData = GetDayContentQueryResponse,
  TQueryKey extends QueryKey = GetDayContentSuspenseQueryKey,
>(
  courseId: GetDayContentPathParams["courseId"],
  dayNumber: GetDayContentPathParams["dayNumber"],
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetDayContentQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey =
    queryOptions?.queryKey ??
    getDayContentSuspenseQueryKey(courseId, dayNumber);

  const query = useSuspenseQuery(
    {
      ...getDayContentSuspenseQueryOptions(courseId, dayNumber, config),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
