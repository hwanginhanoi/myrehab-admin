/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type {
  GetMyAssignedCoursesQueryResponse,
  GetMyAssignedCoursesQueryParams,
} from '../../types/courseProgressController/GetMyAssignedCourses.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getMyAssignedCoursesQueryKey = (
  params: GetMyAssignedCoursesQueryParams
) =>
  [
    { url: '/api/course-progress/my-courses' },
    ...(params ? [params] : []),
  ] as const

export type GetMyAssignedCoursesQueryKey = ReturnType<
  typeof getMyAssignedCoursesQueryKey
>

/**
 * @description Retrieve all courses assigned to me by doctors. Filter by status: PENDING_PURCHASE (not yet paid), ACTIVE (paid, not completed), COMPLETED (finished). Omit status to return all.
 * @summary Get my assigned courses
 * {@link /api/course-progress/my-courses}
 */
export async function getMyAssignedCourses(
  params: GetMyAssignedCoursesQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetMyAssignedCoursesQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: `/api/course-progress/my-courses`,
    params,
    ...requestConfig,
  })
  return res.data
}

export function getMyAssignedCoursesQueryOptions(
  params: GetMyAssignedCoursesQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getMyAssignedCoursesQueryKey(params)
  return queryOptions<
    GetMyAssignedCoursesQueryResponse,
    ResponseErrorConfig<Error>,
    GetMyAssignedCoursesQueryResponse,
    typeof queryKey
  >({
    enabled: !!params,
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getMyAssignedCourses(params, config)
    },
  })
}

/**
 * @description Retrieve all courses assigned to me by doctors. Filter by status: PENDING_PURCHASE (not yet paid), ACTIVE (paid, not completed), COMPLETED (finished). Omit status to return all.
 * @summary Get my assigned courses
 * {@link /api/course-progress/my-courses}
 */
export function useGetMyAssignedCourses<
  TData = GetMyAssignedCoursesQueryResponse,
  TQueryData = GetMyAssignedCoursesQueryResponse,
  TQueryKey extends QueryKey = GetMyAssignedCoursesQueryKey,
>(
  params: GetMyAssignedCoursesQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetMyAssignedCoursesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey =
    queryOptions?.queryKey ?? getMyAssignedCoursesQueryKey(params)

  const query = useQuery(
    {
      ...getMyAssignedCoursesQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
