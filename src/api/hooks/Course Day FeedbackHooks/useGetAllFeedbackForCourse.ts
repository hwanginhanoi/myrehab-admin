/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type {
  GetAllFeedbackForCourseQueryResponse,
  GetAllFeedbackForCourseQueryParams,
} from '../../types/courseDayFeedbackController/GetAllFeedbackForCourse.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getAllFeedbackForCourseQueryKey = (
  params: GetAllFeedbackForCourseQueryParams
) =>
  [
    { url: '/api/course-progress/admin/feedback' },
    ...(params ? [params] : []),
  ] as const

export type GetAllFeedbackForCourseQueryKey = ReturnType<
  typeof getAllFeedbackForCourseQueryKey
>

/**
 * @description Admin/staff view of all daily feedback entries for a course, including all fields (completion %, effort %, pain, hardest exercise, general notes).
 * @summary Get all feedback for a course (admin)
 * {@link /api/course-progress/admin/feedback}
 */
export async function getAllFeedbackForCourse(
  params: GetAllFeedbackForCourseQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetAllFeedbackForCourseQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: `/api/course-progress/admin/feedback`,
    params,
    ...requestConfig,
  })
  return res.data
}

export function getAllFeedbackForCourseQueryOptions(
  params: GetAllFeedbackForCourseQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getAllFeedbackForCourseQueryKey(params)
  return queryOptions<
    GetAllFeedbackForCourseQueryResponse,
    ResponseErrorConfig<Error>,
    GetAllFeedbackForCourseQueryResponse,
    typeof queryKey
  >({
    enabled: !!params,
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getAllFeedbackForCourse(params, config)
    },
  })
}

/**
 * @description Admin/staff view of all daily feedback entries for a course, including all fields (completion %, effort %, pain, hardest exercise, general notes).
 * @summary Get all feedback for a course (admin)
 * {@link /api/course-progress/admin/feedback}
 */
export function useGetAllFeedbackForCourse<
  TData = GetAllFeedbackForCourseQueryResponse,
  TQueryData = GetAllFeedbackForCourseQueryResponse,
  TQueryKey extends QueryKey = GetAllFeedbackForCourseQueryKey,
>(
  params: GetAllFeedbackForCourseQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetAllFeedbackForCourseQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey =
    queryOptions?.queryKey ?? getAllFeedbackForCourseQueryKey(params)

  const query = useQuery(
    {
      ...getAllFeedbackForCourseQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
