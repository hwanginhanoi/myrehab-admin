/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
  GetTopSpendersQueryResponse,
  GetTopSpendersQueryParams,
} from "../../types/reportsController/GetTopSpenders.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getTopSpendersSuspenseQueryKey = (
  params?: GetTopSpendersQueryParams,
) =>
  [{ url: "/api/reports/top-spenders" }, ...(params ? [params] : [])] as const;

export type GetTopSpendersSuspenseQueryKey = ReturnType<
  typeof getTopSpendersSuspenseQueryKey
>;

/**
 * @description Returns the top patients ranked by total amount spent on course purchases, package purchases, and appointment fees. Includes patient name, phone, total spent, transaction count, and last transaction date.
 * @summary Get top spenders
 * {@link /api/reports/top-spenders}
 */
export async function getTopSpendersSuspense(
  params?: GetTopSpendersQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetTopSpendersQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: `/api/reports/top-spenders`,
    params,
    ...requestConfig,
  });
  return res.data;
}

export function getTopSpendersSuspenseQueryOptions(
  params?: GetTopSpendersQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getTopSpendersSuspenseQueryKey(params);
  return queryOptions<
    GetTopSpendersQueryResponse,
    ResponseErrorConfig<Error>,
    GetTopSpendersQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getTopSpendersSuspense(params, config);
    },
  });
}

/**
 * @description Returns the top patients ranked by total amount spent on course purchases, package purchases, and appointment fees. Includes patient name, phone, total spent, transaction count, and last transaction date.
 * @summary Get top spenders
 * {@link /api/reports/top-spenders}
 */
export function useGetTopSpendersSuspense<
  TData = GetTopSpendersQueryResponse,
  TQueryKey extends QueryKey = GetTopSpendersSuspenseQueryKey,
>(
  params?: GetTopSpendersQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetTopSpendersQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey =
    queryOptions?.queryKey ?? getTopSpendersSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...getTopSpendersSuspenseQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
