/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
  GetLowEffortPatientsQueryResponse,
  GetLowEffortPatientsQueryParams,
} from "../../types/reportsController/GetLowEffortPatients.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getLowEffortPatientsSuspenseQueryKey = (
  params?: GetLowEffortPatientsQueryParams,
) =>
  [
    { url: "/api/reports/low-effort-patients" },
    ...(params ? [params] : []),
  ] as const;

export type GetLowEffortPatientsSuspenseQueryKey = ReturnType<
  typeof getLowEffortPatientsSuspenseQueryKey
>;

/**
 * @description Returns patients whose average effort percentage across all logged course days is below a configurable threshold. Sorted by effort ascending (lowest effort first). Useful for flagging disengaged patients for doctor review.
 * @summary Get low effort patients
 * {@link /api/reports/low-effort-patients}
 */
export async function getLowEffortPatientsSuspense(
  params?: GetLowEffortPatientsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetLowEffortPatientsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: `/api/reports/low-effort-patients`,
    params,
    ...requestConfig,
  });
  return res.data;
}

export function getLowEffortPatientsSuspenseQueryOptions(
  params?: GetLowEffortPatientsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getLowEffortPatientsSuspenseQueryKey(params);
  return queryOptions<
    GetLowEffortPatientsQueryResponse,
    ResponseErrorConfig<Error>,
    GetLowEffortPatientsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getLowEffortPatientsSuspense(params, config);
    },
  });
}

/**
 * @description Returns patients whose average effort percentage across all logged course days is below a configurable threshold. Sorted by effort ascending (lowest effort first). Useful for flagging disengaged patients for doctor review.
 * @summary Get low effort patients
 * {@link /api/reports/low-effort-patients}
 */
export function useGetLowEffortPatientsSuspense<
  TData = GetLowEffortPatientsQueryResponse,
  TQueryKey extends QueryKey = GetLowEffortPatientsSuspenseQueryKey,
>(
  params?: GetLowEffortPatientsQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetLowEffortPatientsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey =
    queryOptions?.queryKey ?? getLowEffortPatientsSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...getLowEffortPatientsSuspenseQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
