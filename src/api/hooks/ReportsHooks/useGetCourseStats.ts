/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetCourseStatsQueryResponse } from "../../types/reportsController/GetCourseStats.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getCourseStatsQueryKey = () => [{ url: '/api/reports/course-stats' }] as const

export type GetCourseStatsQueryKey = ReturnType<typeof getCourseStatsQueryKey>

/**
 * @description Returns per-course statistics including total assigned, started, in-progress, completed, dropped, and completion rate. Results are sorted by completion rate (highest first).
 * @summary Get course engagement statistics
 * {@link /api/reports/course-stats}
 */
export async function getCourseStats(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetCourseStatsQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : `/api/reports/course-stats`, ... requestConfig })  
  return res.data
}

export function getCourseStatsQueryOptions(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const queryKey = getCourseStatsQueryKey()
  return queryOptions<GetCourseStatsQueryResponse, ResponseErrorConfig<Error>, GetCourseStatsQueryResponse, typeof queryKey>({
 
   queryKey,
   queryFn: async ({ signal }) => {
      config.signal = signal
      return getCourseStats(config)
   },
  })
}

/**
 * @description Returns per-course statistics including total assigned, started, in-progress, completed, dropped, and completion rate. Results are sorted by completion rate (highest first).
 * @summary Get course engagement statistics
 * {@link /api/reports/course-stats}
 */
export function useGetCourseStats<TData = GetCourseStatsQueryResponse, TQueryData = GetCourseStatsQueryResponse, TQueryKey extends QueryKey = GetCourseStatsQueryKey>(options: 
{
  query?: Partial<QueryObserverOptions<GetCourseStatsQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<RequestConfig> & { client?: typeof fetch }
}
 = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getCourseStatsQueryKey()

  const query = useQuery({
   ...getCourseStatsQueryOptions(config),
   queryKey,
   ...queryOptions
  } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}