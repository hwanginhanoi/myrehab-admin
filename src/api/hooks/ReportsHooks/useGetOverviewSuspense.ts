/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetOverviewQueryResponse } from "../../types/reportsController/GetOverview.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type { QueryKey, QueryClient, UseSuspenseQueryOptions, UseSuspenseQueryResult } from "@tanstack/react-query";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getOverviewSuspenseQueryKey = () => [{ url: '/api/reports/overview' }] as const

export type GetOverviewSuspenseQueryKey = ReturnType<typeof getOverviewSuspenseQueryKey>

/**
 * @description Returns all platform-level KPI values in one call: total patients, new patients this month, total revenue this month, active subscriptions, appointments today, overall course completion rate, last 30 days revenue trend sparkline, and today's appointment status breakdown.
 * @summary Get overview KPIs
 * {@link /api/reports/overview}
 */
export async function getOverviewSuspense(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetOverviewQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : `/api/reports/overview`, ... requestConfig })  
  return res.data
}

export function getOverviewSuspenseQueryOptions(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const queryKey = getOverviewSuspenseQueryKey()
  return queryOptions<GetOverviewQueryResponse, ResponseErrorConfig<Error>, GetOverviewQueryResponse, typeof queryKey>({
 
   queryKey,
   queryFn: async ({ signal }) => {
      config.signal = signal
      return getOverviewSuspense(config)
   },
  })
}

/**
 * @description Returns all platform-level KPI values in one call: total patients, new patients this month, total revenue this month, active subscriptions, appointments today, overall course completion rate, last 30 days revenue trend sparkline, and today's appointment status breakdown.
 * @summary Get overview KPIs
 * {@link /api/reports/overview}
 */
export function useGetOverviewSuspense<TData = GetOverviewQueryResponse, TQueryKey extends QueryKey = GetOverviewSuspenseQueryKey>(options: 
{
  query?: Partial<UseSuspenseQueryOptions<GetOverviewQueryResponse, ResponseErrorConfig<Error>, TData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<RequestConfig> & { client?: typeof fetch }
}
 = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getOverviewSuspenseQueryKey()

  const query = useSuspenseQuery({
   ...getOverviewSuspenseQueryOptions(config),
   queryKey,
   ...queryOptions
  } as unknown as UseSuspenseQueryOptions, queryClient) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}