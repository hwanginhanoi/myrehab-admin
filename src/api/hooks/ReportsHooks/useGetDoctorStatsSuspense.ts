/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type { GetDoctorStatsQueryResponse } from "../../types/reportsController/GetDoctorStats.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getDoctorStatsSuspenseQueryKey = () =>
  [{ url: "/api/reports/doctor-stats" }] as const;

export type GetDoctorStatsSuspenseQueryKey = ReturnType<
  typeof getDoctorStatsSuspenseQueryKey
>;

/**
 * @description Returns per-doctor statistics including patient count, courses assigned, courses completed by their patients, completion rate, appointments this month, and dispute count. Sorted by patient caseload (highest first).
 * @summary Get doctor performance statistics
 * {@link /api/reports/doctor-stats}
 */
export async function getDoctorStatsSuspense(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetDoctorStatsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: "GET", url: `/api/reports/doctor-stats`, ...requestConfig });
  return res.data;
}

export function getDoctorStatsSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getDoctorStatsSuspenseQueryKey();
  return queryOptions<
    GetDoctorStatsQueryResponse,
    ResponseErrorConfig<Error>,
    GetDoctorStatsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getDoctorStatsSuspense(config);
    },
  });
}

/**
 * @description Returns per-doctor statistics including patient count, courses assigned, courses completed by their patients, completion rate, appointments this month, and dispute count. Sorted by patient caseload (highest first).
 * @summary Get doctor performance statistics
 * {@link /api/reports/doctor-stats}
 */
export function useGetDoctorStatsSuspense<
  TData = GetDoctorStatsQueryResponse,
  TQueryKey extends QueryKey = GetDoctorStatsSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetDoctorStatsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey = queryOptions?.queryKey ?? getDoctorStatsSuspenseQueryKey();

  const query = useSuspenseQuery(
    {
      ...getDoctorStatsSuspenseQueryOptions(config),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
