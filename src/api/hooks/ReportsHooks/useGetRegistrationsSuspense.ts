/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
  GetRegistrationsQueryResponse,
  GetRegistrationsQueryParams,
} from "../../types/reportsController/GetRegistrations.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getRegistrationsSuspenseQueryKey = (
  params?: GetRegistrationsQueryParams,
) =>
  [{ url: "/api/reports/registrations" }, ...(params ? [params] : [])] as const;

export type GetRegistrationsSuspenseQueryKey = ReturnType<
  typeof getRegistrationsSuspenseQueryKey
>;

/**
 * @description Returns patient registration KPIs and time-series data grouped by day, week, or month. Includes total patients, new this month/week, gender breakdown, and registration trend chart data. Defaults to last 30 days if no date range is provided.
 * @summary Get patient registration analytics
 * {@link /api/reports/registrations}
 */
export async function getRegistrationsSuspense(
  params?: GetRegistrationsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetRegistrationsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: `/api/reports/registrations`,
    params,
    ...requestConfig,
  });
  return res.data;
}

export function getRegistrationsSuspenseQueryOptions(
  params?: GetRegistrationsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getRegistrationsSuspenseQueryKey(params);
  return queryOptions<
    GetRegistrationsQueryResponse,
    ResponseErrorConfig<Error>,
    GetRegistrationsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getRegistrationsSuspense(params, config);
    },
  });
}

/**
 * @description Returns patient registration KPIs and time-series data grouped by day, week, or month. Includes total patients, new this month/week, gender breakdown, and registration trend chart data. Defaults to last 30 days if no date range is provided.
 * @summary Get patient registration analytics
 * {@link /api/reports/registrations}
 */
export function useGetRegistrationsSuspense<
  TData = GetRegistrationsQueryResponse,
  TQueryKey extends QueryKey = GetRegistrationsSuspenseQueryKey,
>(
  params?: GetRegistrationsQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetRegistrationsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey =
    queryOptions?.queryKey ?? getRegistrationsSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...getRegistrationsSuspenseQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
