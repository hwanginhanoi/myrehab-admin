/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
  GetAppointmentStatsQueryResponse,
  GetAppointmentStatsQueryParams,
} from "../../types/reportsController/GetAppointmentStats.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getAppointmentStatsQueryKey = (
  params?: GetAppointmentStatsQueryParams,
) =>
  [
    { url: "/api/reports/appointment-stats" },
    ...(params ? [params] : []),
  ] as const;

export type GetAppointmentStatsQueryKey = ReturnType<
  typeof getAppointmentStatsQueryKey
>;

/**
 * @description Returns appointment KPIs (total, completion rate, cancellation rate, dispute rate, revenue) and time-series data. Also includes status distribution and bookings by day of week. Defaults to last 30 days if no date range is provided.
 * @summary Get appointment analytics
 * {@link /api/reports/appointment-stats}
 */
export async function getAppointmentStats(
  params?: GetAppointmentStatsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetAppointmentStatsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: `/api/reports/appointment-stats`,
    params,
    ...requestConfig,
  });
  return res.data;
}

export function getAppointmentStatsQueryOptions(
  params?: GetAppointmentStatsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getAppointmentStatsQueryKey(params);
  return queryOptions<
    GetAppointmentStatsQueryResponse,
    ResponseErrorConfig<Error>,
    GetAppointmentStatsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getAppointmentStats(params, config);
    },
  });
}

/**
 * @description Returns appointment KPIs (total, completion rate, cancellation rate, dispute rate, revenue) and time-series data. Also includes status distribution and bookings by day of week. Defaults to last 30 days if no date range is provided.
 * @summary Get appointment analytics
 * {@link /api/reports/appointment-stats}
 */
export function useGetAppointmentStats<
  TData = GetAppointmentStatsQueryResponse,
  TQueryData = GetAppointmentStatsQueryResponse,
  TQueryKey extends QueryKey = GetAppointmentStatsQueryKey,
>(
  params?: GetAppointmentStatsQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetAppointmentStatsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey =
    queryOptions?.queryKey ?? getAppointmentStatsQueryKey(params);

  const query = useQuery(
    {
      ...getAppointmentStatsQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
