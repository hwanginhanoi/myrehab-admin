/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type { GetOverviewQueryResponse } from "../../types/reportsController/GetOverview.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getOverviewQueryKey = () =>
  [{ url: "/api/reports/overview" }] as const;

export type GetOverviewQueryKey = ReturnType<typeof getOverviewQueryKey>;

/**
 * @description Returns all platform-level KPI values in one call: total patients, new patients this month, total revenue this month, active subscriptions, appointments today, overall course completion rate, last 30 days revenue trend sparkline, and today's appointment status breakdown.
 * @summary Get overview KPIs
 * {@link /api/reports/overview}
 */
export async function getOverview(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetOverviewQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: "GET", url: `/api/reports/overview`, ...requestConfig });
  return res.data;
}

export function getOverviewQueryOptions(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getOverviewQueryKey();
  return queryOptions<
    GetOverviewQueryResponse,
    ResponseErrorConfig<Error>,
    GetOverviewQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getOverview(config);
    },
  });
}

/**
 * @description Returns all platform-level KPI values in one call: total patients, new patients this month, total revenue this month, active subscriptions, appointments today, overall course completion rate, last 30 days revenue trend sparkline, and today's appointment status breakdown.
 * @summary Get overview KPIs
 * {@link /api/reports/overview}
 */
export function useGetOverview<
  TData = GetOverviewQueryResponse,
  TQueryData = GetOverviewQueryResponse,
  TQueryKey extends QueryKey = GetOverviewQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetOverviewQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey = queryOptions?.queryKey ?? getOverviewQueryKey();

  const query = useQuery(
    {
      ...getOverviewQueryOptions(config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
