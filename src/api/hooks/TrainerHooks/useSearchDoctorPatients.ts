/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type {
  SearchDoctorPatientsQueryResponse,
  SearchDoctorPatientsQueryParams,
} from '../../types/trainerController/SearchDoctorPatients.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const searchDoctorPatientsQueryKey = (
  params?: SearchDoctorPatientsQueryParams
) =>
  [
    { url: '/api/trainer/patients/search' },
    ...(params ? [params] : []),
  ] as const

export type SearchDoctorPatientsQueryKey = ReturnType<
  typeof searchDoctorPatientsQueryKey
>

/**
 * @description Returns up to 5 closest matching patients assigned to the trainer's supervising doctor, based on name or phone number.
 * @summary Search doctor's patients (top 5 results)
 * {@link /api/trainer/patients/search}
 */
export async function searchDoctorPatients(
  params?: SearchDoctorPatientsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    SearchDoctorPatientsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: `/api/trainer/patients/search`,
    params,
    ...requestConfig,
  })
  return res.data
}

export function searchDoctorPatientsQueryOptions(
  params?: SearchDoctorPatientsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = searchDoctorPatientsQueryKey(params)
  return queryOptions<
    SearchDoctorPatientsQueryResponse,
    ResponseErrorConfig<Error>,
    SearchDoctorPatientsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return searchDoctorPatients(params, config)
    },
  })
}

/**
 * @description Returns up to 5 closest matching patients assigned to the trainer's supervising doctor, based on name or phone number.
 * @summary Search doctor's patients (top 5 results)
 * {@link /api/trainer/patients/search}
 */
export function useSearchDoctorPatients<
  TData = SearchDoctorPatientsQueryResponse,
  TQueryData = SearchDoctorPatientsQueryResponse,
  TQueryKey extends QueryKey = SearchDoctorPatientsQueryKey,
>(
  params?: SearchDoctorPatientsQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        SearchDoctorPatientsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey =
    queryOptions?.queryKey ?? searchDoctorPatientsQueryKey(params)

  const query = useQuery(
    {
      ...searchDoctorPatientsQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
