/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type {
  SearchDoctorPatientsQueryResponse,
  SearchDoctorPatientsQueryParams,
} from '../../types/trainerController/SearchDoctorPatients.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query'

export const searchDoctorPatientsSuspenseQueryKey = (
  params?: SearchDoctorPatientsQueryParams
) =>
  [
    { url: '/api/trainer/patients/search' },
    ...(params ? [params] : []),
  ] as const

export type SearchDoctorPatientsSuspenseQueryKey = ReturnType<
  typeof searchDoctorPatientsSuspenseQueryKey
>

/**
 * @description Returns up to 5 closest matching patients assigned to the trainer's supervising doctor, based on name or phone number.
 * @summary Search doctor's patients (top 5 results)
 * {@link /api/trainer/patients/search}
 */
export async function searchDoctorPatientsSuspense(
  params?: SearchDoctorPatientsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    SearchDoctorPatientsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: `/api/trainer/patients/search`,
    params,
    ...requestConfig,
  })
  return res.data
}

export function searchDoctorPatientsSuspenseQueryOptions(
  params?: SearchDoctorPatientsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = searchDoctorPatientsSuspenseQueryKey(params)
  return queryOptions<
    SearchDoctorPatientsQueryResponse,
    ResponseErrorConfig<Error>,
    SearchDoctorPatientsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return searchDoctorPatientsSuspense(params, config)
    },
  })
}

/**
 * @description Returns up to 5 closest matching patients assigned to the trainer's supervising doctor, based on name or phone number.
 * @summary Search doctor's patients (top 5 results)
 * {@link /api/trainer/patients/search}
 */
export function useSearchDoctorPatientsSuspense<
  TData = SearchDoctorPatientsQueryResponse,
  TQueryKey extends QueryKey = SearchDoctorPatientsSuspenseQueryKey,
>(
  params?: SearchDoctorPatientsQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        SearchDoctorPatientsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey =
    queryOptions?.queryKey ?? searchDoctorPatientsSuspenseQueryKey(params)

  const query = useSuspenseQuery(
    {
      ...searchDoctorPatientsSuspenseQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
