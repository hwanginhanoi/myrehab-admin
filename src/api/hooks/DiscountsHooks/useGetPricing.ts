/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetPricingQueryResponse } from "../../types/discountsController/GetPricing.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getPricingQueryKey = () => [{ url: '/api/discounts/pricing' }] as const

export type GetPricingQueryKey = ReturnType<typeof getPricingQueryKey>

/**
 * @description Returns all service types with original price, active discount info, and final price. Available to all authenticated users.
 * @summary Get current prices with discounts
 * {@link /api/discounts/pricing}
 */
export async function getPricing(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetPricingQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : `/api/discounts/pricing`, ... requestConfig })  
  return res.data
}

export function getPricingQueryOptions(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const queryKey = getPricingQueryKey()
  return queryOptions<GetPricingQueryResponse, ResponseErrorConfig<Error>, GetPricingQueryResponse, typeof queryKey>({
 
   queryKey,
   queryFn: async ({ signal }) => {
      config.signal = signal
      return getPricing(config)
   },
  })
}

/**
 * @description Returns all service types with original price, active discount info, and final price. Available to all authenticated users.
 * @summary Get current prices with discounts
 * {@link /api/discounts/pricing}
 */
export function useGetPricing<TData = GetPricingQueryResponse, TQueryData = GetPricingQueryResponse, TQueryKey extends QueryKey = GetPricingQueryKey>(options: 
{
  query?: Partial<QueryObserverOptions<GetPricingQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<RequestConfig> & { client?: typeof fetch }
}
 = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getPricingQueryKey()

  const query = useQuery({
   ...getPricingQueryOptions(config),
   queryKey,
   ...queryOptions
  } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}