/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";
import type {
  UseMutationOptions,
  UseMutationResult,
  QueryClient,
} from "@tanstack/react-query";
import type {
  GenerateDownloadUrlMutationRequest,
  GenerateDownloadUrlMutationResponse,
} from "../../types/fileManagementController/GenerateDownloadUrl.ts";
import { mutationOptions, useMutation } from "@tanstack/react-query";

export const generateDownloadUrlMutationKey = () =>
  [{ url: "/api/files/presigned-download-url" }] as const;

export type GenerateDownloadUrlMutationKey = ReturnType<
  typeof generateDownloadUrlMutationKey
>;

/**
 * @description Generate a temporary URL for viewing private videos. This is used for videos that require authentication.
 * @summary Generate presigned URL for video download
 * {@link /api/files/presigned-download-url}
 */
export async function generateDownloadUrl(
  data: GenerateDownloadUrlMutationRequest,
  config: Partial<RequestConfig<GenerateDownloadUrlMutationRequest>> & {
    client?: typeof fetch;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = data;

  const res = await request<
    GenerateDownloadUrlMutationResponse,
    ResponseErrorConfig<Error>,
    GenerateDownloadUrlMutationRequest
  >({
    method: "POST",
    url: `/api/files/presigned-download-url`,
    data: requestData,
    ...requestConfig,
  });
  return res.data;
}

export function generateDownloadUrlMutationOptions(
  config: Partial<RequestConfig<GenerateDownloadUrlMutationRequest>> & {
    client?: typeof fetch;
  } = {},
) {
  const mutationKey = generateDownloadUrlMutationKey();
  return mutationOptions<
    GenerateDownloadUrlMutationResponse,
    ResponseErrorConfig<Error>,
    { data: GenerateDownloadUrlMutationRequest },
    typeof mutationKey
  >({
    mutationKey,
    mutationFn: async ({ data }) => {
      return generateDownloadUrl(data, config);
    },
  });
}

/**
 * @description Generate a temporary URL for viewing private videos. This is used for videos that require authentication.
 * @summary Generate presigned URL for video download
 * {@link /api/files/presigned-download-url}
 */
export function useGenerateDownloadUrl<TContext>(
  options: {
    mutation?: UseMutationOptions<
      GenerateDownloadUrlMutationResponse,
      ResponseErrorConfig<Error>,
      { data: GenerateDownloadUrlMutationRequest },
      TContext
    > & { client?: QueryClient };
    client?: Partial<RequestConfig<GenerateDownloadUrlMutationRequest>> & {
      client?: typeof fetch;
    };
  } = {},
) {
  const { mutation = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...mutationOptions } = mutation;
  const mutationKey =
    mutationOptions.mutationKey ?? generateDownloadUrlMutationKey();

  const baseOptions = generateDownloadUrlMutationOptions(
    config,
  ) as UseMutationOptions<
    GenerateDownloadUrlMutationResponse,
    ResponseErrorConfig<Error>,
    { data: GenerateDownloadUrlMutationRequest },
    TContext
  >;

  return useMutation<
    GenerateDownloadUrlMutationResponse,
    ResponseErrorConfig<Error>,
    { data: GenerateDownloadUrlMutationRequest },
    TContext
  >(
    {
      ...baseOptions,
      mutationKey,
      ...mutationOptions,
    },
    queryClient,
  ) as UseMutationResult<
    GenerateDownloadUrlMutationResponse,
    ResponseErrorConfig<Error>,
    { data: GenerateDownloadUrlMutationRequest },
    TContext
  >;
}
