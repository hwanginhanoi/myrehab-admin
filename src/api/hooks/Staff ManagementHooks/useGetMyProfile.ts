/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type { GetMyProfileQueryResponse } from '../../types/staffManagementController/GetMyProfile.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getMyProfileQueryKey = () =>
  [{ url: '/api/admin/staff/me' }] as const

export type GetMyProfileQueryKey = ReturnType<typeof getMyProfileQueryKey>

/**
 * @description Retrieve the authenticated staff member's own profile information
 * @summary Get my profile
 * {@link /api/admin/staff/me}
 */
export async function getMyProfile(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetMyProfileQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: 'GET', url: `/api/admin/staff/me`, ...requestConfig })
  return res.data
}

export function getMyProfileQueryOptions(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getMyProfileQueryKey()
  return queryOptions<
    GetMyProfileQueryResponse,
    ResponseErrorConfig<Error>,
    GetMyProfileQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getMyProfile(config)
    },
  })
}

/**
 * @description Retrieve the authenticated staff member's own profile information
 * @summary Get my profile
 * {@link /api/admin/staff/me}
 */
export function useGetMyProfile<
  TData = GetMyProfileQueryResponse,
  TQueryData = GetMyProfileQueryResponse,
  TQueryKey extends QueryKey = GetMyProfileQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetMyProfileQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getMyProfileQueryKey()

  const query = useQuery(
    {
      ...getMyProfileQueryOptions(config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
