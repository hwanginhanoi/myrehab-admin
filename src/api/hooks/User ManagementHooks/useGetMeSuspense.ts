/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type { GetMeQueryResponse } from '../../types/userManagementController/GetMe.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query'

export const getMeSuspenseQueryKey = () => [{ url: '/api/users/me' }] as const

export type GetMeSuspenseQueryKey = ReturnType<typeof getMeSuspenseQueryKey>

/**
 * @description Retrieve the authenticated mobile user's own basic information (name, gender, date of birth, phone, email)
 * @summary Get my profile
 * {@link /api/users/me}
 */
export async function getMeSuspense(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetMeQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: 'GET', url: `/api/users/me`, ...requestConfig })
  return res.data
}

export function getMeSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getMeSuspenseQueryKey()
  return queryOptions<
    GetMeQueryResponse,
    ResponseErrorConfig<Error>,
    GetMeQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getMeSuspense(config)
    },
  })
}

/**
 * @description Retrieve the authenticated mobile user's own basic information (name, gender, date of birth, phone, email)
 * @summary Get my profile
 * {@link /api/users/me}
 */
export function useGetMeSuspense<
  TData = GetMeQueryResponse,
  TQueryKey extends QueryKey = GetMeSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetMeQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getMeSuspenseQueryKey()

  const query = useSuspenseQuery(
    {
      ...getMeSuspenseQueryOptions(config),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
