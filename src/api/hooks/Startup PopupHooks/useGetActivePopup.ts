/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type { GetActivePopupQueryResponse } from '../../types/startupPopupController/GetActivePopup.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getActivePopupQueryKey = () =>
  [{ url: '/api/startup-popup/active' }] as const

export type GetActivePopupQueryKey = ReturnType<typeof getActivePopupQueryKey>

/**
 * @description Returns a list with the active popup, or empty list if none. Public endpoint used by the mobile app on startup.
 * @summary Get active startup popup
 * {@link /api/startup-popup/active}
 */
export async function getActivePopup(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetActivePopupQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: 'GET', url: `/api/startup-popup/active`, ...requestConfig })
  return res.data
}

export function getActivePopupQueryOptions(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getActivePopupQueryKey()
  return queryOptions<
    GetActivePopupQueryResponse,
    ResponseErrorConfig<Error>,
    GetActivePopupQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getActivePopup(config)
    },
  })
}

/**
 * @description Returns a list with the active popup, or empty list if none. Public endpoint used by the mobile app on startup.
 * @summary Get active startup popup
 * {@link /api/startup-popup/active}
 */
export function useGetActivePopup<
  TData = GetActivePopupQueryResponse,
  TQueryData = GetActivePopupQueryResponse,
  TQueryKey extends QueryKey = GetActivePopupQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetActivePopupQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getActivePopupQueryKey()

  const query = useQuery(
    {
      ...getActivePopupQueryOptions(config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
