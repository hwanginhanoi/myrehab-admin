/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type {
  DeletePopupMutationResponse,
  DeletePopupPathParams,
} from '../../types/startupPopupController/DeletePopup.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  UseMutationOptions,
  UseMutationResult,
  QueryClient,
} from '@tanstack/react-query'
import { mutationOptions, useMutation } from '@tanstack/react-query'

export const deletePopupMutationKey = () =>
  [{ url: '/api/startup-popup/:id' }] as const

export type DeletePopupMutationKey = ReturnType<typeof deletePopupMutationKey>

/**
 * @summary Delete startup popup
 * {@link /api/startup-popup/:id}
 */
export async function deletePopup(
  id: DeletePopupPathParams['id'],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    DeletePopupMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: 'DELETE', url: `/api/startup-popup/${id}`, ...requestConfig })
  return res.data
}

export function deletePopupMutationOptions(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const mutationKey = deletePopupMutationKey()
  return mutationOptions<
    DeletePopupMutationResponse,
    ResponseErrorConfig<Error>,
    { id: DeletePopupPathParams['id'] },
    typeof mutationKey
  >({
    mutationKey,
    mutationFn: async ({ id }) => {
      return deletePopup(id, config)
    },
  })
}

/**
 * @summary Delete startup popup
 * {@link /api/startup-popup/:id}
 */
export function useDeletePopup<TContext>(
  options: {
    mutation?: UseMutationOptions<
      DeletePopupMutationResponse,
      ResponseErrorConfig<Error>,
      { id: DeletePopupPathParams['id'] },
      TContext
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { mutation = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...mutationOptions } = mutation
  const mutationKey = mutationOptions.mutationKey ?? deletePopupMutationKey()

  const baseOptions = deletePopupMutationOptions(config) as UseMutationOptions<
    DeletePopupMutationResponse,
    ResponseErrorConfig<Error>,
    { id: DeletePopupPathParams['id'] },
    TContext
  >

  return useMutation<
    DeletePopupMutationResponse,
    ResponseErrorConfig<Error>,
    { id: DeletePopupPathParams['id'] },
    TContext
  >(
    {
      ...baseOptions,
      mutationKey,
      ...mutationOptions,
    },
    queryClient
  ) as UseMutationResult<
    DeletePopupMutationResponse,
    ResponseErrorConfig<Error>,
    { id: DeletePopupPathParams['id'] },
    TContext
  >
}
