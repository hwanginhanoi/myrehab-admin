/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client'
import type { GetActivePopupQueryResponse } from '../../types/startupPopupController/GetActivePopup.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api-client'
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query'

export const getActivePopupSuspenseQueryKey = () =>
  [{ url: '/api/startup-popup/active' }] as const

export type GetActivePopupSuspenseQueryKey = ReturnType<
  typeof getActivePopupSuspenseQueryKey
>

/**
 * @description Returns the currently active popup, or 204 if none. Public endpoint used by the mobile app on startup.
 * @summary Get active startup popup
 * {@link /api/startup-popup/active}
 */
export async function getActivePopupSuspense(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetActivePopupQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: 'GET', url: `/api/startup-popup/active`, ...requestConfig })
  return res.data
}

export function getActivePopupSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getActivePopupSuspenseQueryKey()
  return queryOptions<
    GetActivePopupQueryResponse,
    ResponseErrorConfig<Error>,
    GetActivePopupQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getActivePopupSuspense(config)
    },
  })
}

/**
 * @description Returns the currently active popup, or 204 if none. Public endpoint used by the mobile app on startup.
 * @summary Get active startup popup
 * {@link /api/startup-popup/active}
 */
export function useGetActivePopupSuspense<
  TData = GetActivePopupQueryResponse,
  TQueryKey extends QueryKey = GetActivePopupSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetActivePopupQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getActivePopupSuspenseQueryKey()

  const query = useSuspenseQuery(
    {
      ...getActivePopupSuspenseQueryOptions(config),
      queryKey,
      ...queryOptions,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
