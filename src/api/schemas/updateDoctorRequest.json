{
  "allOf": [
    { "$ref": "#/components/schemas/UpdateStaffRequest" },
    {
      "type": "object",
      "properties": {
        "specialization": {
          "type": "string",
          "description": "Doctor's medical specialization",
          "example": "Orthopedics"
        },
        "isPublic": {
          "type": "boolean",
          "description": "Whether this doctor is visible in the mobile app"
        }
      }
    }
  ],
  "description": "Update request for Doctor staff",
  "required": ["email", "fullName", "staffType"],
  "x-readme-ref-name": "UpdateDoctorRequest"
}
