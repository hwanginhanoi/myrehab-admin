/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { UpdateGroupMutationRequest, UpdateGroupMutationResponse, UpdateGroupPathParams } from "../../types/exerciseGroupsController/UpdateGroup.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getUpdateGroupUrl({ id }: { id: UpdateGroupPathParams["id"] }) {
  const res = { method: 'PUT', url: `/api/exercise-groups/${id}` as const }  
  return res
}

/**
 * @description Update an existing exercise group
 * @summary Update exercise group
 * {@link /api/exercise-groups/:id}
 */
export async function updateGroup({ id }: { id: UpdateGroupPathParams["id"] }, data: UpdateGroupMutationRequest, config: Partial<RequestConfig<UpdateGroupMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<UpdateGroupMutationResponse, ResponseErrorConfig<Error>, UpdateGroupMutationRequest>({ method : "PUT", url : getUpdateGroupUrl({ id }).url.toString(), data : requestData, ... requestConfig })  
  return res.data
}