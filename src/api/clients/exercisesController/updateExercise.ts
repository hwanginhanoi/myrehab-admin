/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { UpdateExerciseMutationRequest, UpdateExerciseMutationResponse, UpdateExercisePathParams } from "../../types/exercisesController/UpdateExercise.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getUpdateExerciseUrl({ id }: { id: UpdateExercisePathParams["id"] }) {
  const res = { method: 'PUT', url: `/api/exercises/${id}` as const }  
  return res
}

/**
 * @description Update an existing exercise with multiple categories and groups
 * @summary Update exercise
 * {@link /api/exercises/:id}
 */
export async function updateExercise({ id }: { id: UpdateExercisePathParams["id"] }, data: UpdateExerciseMutationRequest, config: Partial<RequestConfig<UpdateExerciseMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<UpdateExerciseMutationResponse, ResponseErrorConfig<Error>, UpdateExerciseMutationRequest>({ method : "PUT", url : getUpdateExerciseUrl({ id }).url.toString(), data : requestData, ... requestConfig })  
  return res.data
}