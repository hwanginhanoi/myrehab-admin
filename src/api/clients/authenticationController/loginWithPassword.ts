/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { LoginWithPasswordMutationRequest, LoginWithPasswordMutationResponse } from "../../types/authenticationController/LoginWithPassword.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getLoginWithPasswordUrl() {
  const res = { method: 'POST', url: `/api/auth/staff/login` as const }  
  return res
}

/**
 * @description Authenticate staff (Doctor, Admin, SuperAdmin, Trainer) using email and password. Returns access token only (no refresh token).
 * @summary Staff login with password
 * {@link /api/auth/staff/login}
 */
export async function loginWithPassword(data: LoginWithPasswordMutationRequest, config: Partial<RequestConfig<LoginWithPasswordMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<LoginWithPasswordMutationResponse, ResponseErrorConfig<Error>, LoginWithPasswordMutationRequest>({ method : "POST", url : getLoginWithPasswordUrl().url.toString(), data : requestData, ... requestConfig })  
  return res.data
}