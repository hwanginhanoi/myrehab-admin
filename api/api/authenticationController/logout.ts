/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { LogoutMutationRequest, LogoutMutationResponse } from "../../types/Logout.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getLogoutUrl() {
  const res = { method: 'POST', url: `/api/auth/logout` as const }  
  return res
}

/**
 * @description Revoke refresh token and logout user
 * @summary Logout user
 * {@link /api/auth/logout}
 */
export async function logout(data: LogoutMutationRequest, config: Partial<RequestConfig<LogoutMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<LogoutMutationResponse, ResponseErrorConfig<Error>, LogoutMutationRequest>({ method : "POST", url : getLogoutUrl().url.toString(), data : requestData, ... requestConfig })  
  return res.data
}