/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetUserBalanceQueryResponse } from "../../types/GetUserBalance.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getGetUserBalanceUrl() {
  const res = { method: 'GET', url: `/api/balance` as const }  
  return res
}

/**
 * @description Get current user's balance
 * @summary Get user balance
 * {@link /api/balance}
 */
export async function getUserBalance(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetUserBalanceQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGetUserBalanceUrl().url.toString(), ... requestConfig })  
  return res.data
}