/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetActivityStatsQueryResponse } from "../../types/GetActivityStats.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getGetActivityStatsUrl() {
  const res = { method: 'GET', url: `/api/activity/stats` as const }  
  return res
}

/**
 * @description Get activity stats including streaks, total exercises, and active days
 * @summary Get activity statistics
 * {@link /api/activity/stats}
 */
export async function getActivityStats(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetActivityStatsQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGetActivityStatsUrl().url.toString(), ... requestConfig })  
  return res.data
}