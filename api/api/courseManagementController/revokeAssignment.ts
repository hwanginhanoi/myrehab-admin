/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { RevokeAssignmentMutationResponse, RevokeAssignmentPathParams } from "../../types/RevokeAssignment.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getRevokeAssignmentUrl(assignmentId: RevokeAssignmentPathParams["assignmentId"]) {
  const res = { method: 'DELETE', url: `/api/courses/assignments/${assignmentId}` as const }  
  return res
}

/**
 * @description Revoke a course assignment. Only assigned courses can be revoked, not purchased courses.
 * @summary Revoke an assignment
 * {@link /api/courses/assignments/:assignmentId}
 */
export async function revokeAssignment(assignmentId: RevokeAssignmentPathParams["assignmentId"], config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<RevokeAssignmentMutationResponse, ResponseErrorConfig<Error>, unknown>({ method : "DELETE", url : getRevokeAssignmentUrl(assignmentId).url.toString(), ... requestConfig })  
  return res.data
}