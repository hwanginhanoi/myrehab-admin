/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { UpdateCourseMutationRequest, UpdateCourseMutationResponse, UpdateCoursePathParams } from "../../types/UpdateCourse.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getUpdateCourseUrl(id: UpdateCoursePathParams["id"]) {
  const res = { method: 'PUT', url: `/api/courses/${id}` as const }  
  return res
}

/**
 * @description Update an existing rehabilitation course (Admin and Doctor only)
 * @summary Update course
 * {@link /api/courses/:id}
 */
export async function updateCourse(id: UpdateCoursePathParams["id"], data: UpdateCourseMutationRequest, config: Partial<RequestConfig<UpdateCourseMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<UpdateCourseMutationResponse, ResponseErrorConfig<Error>, UpdateCourseMutationRequest>({ method : "PUT", url : getUpdateCourseUrl(id).url.toString(), data : requestData, ... requestConfig })  
  return res.data
}