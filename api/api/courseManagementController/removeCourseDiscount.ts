/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { RemoveCourseDiscountMutationResponse, RemoveCourseDiscountPathParams } from "../../types/RemoveCourseDiscount.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getRemoveCourseDiscountUrl(id: RemoveCourseDiscountPathParams["id"]) {
  const res = { method: 'DELETE', url: `/api/courses/${id}/discount` as const }  
  return res
}

/**
 * @description Remove discount from a course (Admin only)
 * @summary Remove course discount
 * {@link /api/courses/:id/discount}
 */
export async function removeCourseDiscount(id: RemoveCourseDiscountPathParams["id"], config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<RemoveCourseDiscountMutationResponse, ResponseErrorConfig<Error>, unknown>({ method : "DELETE", url : getRemoveCourseDiscountUrl(id).url.toString(), ... requestConfig })  
  return res.data
}