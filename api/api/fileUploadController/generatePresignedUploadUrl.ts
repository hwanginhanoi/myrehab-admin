/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GeneratePresignedUploadUrlMutationRequest, GeneratePresignedUploadUrlMutationResponse } from "../../types/GeneratePresignedUploadUrl.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getGeneratePresignedUploadUrlUrl() {
  const res = { method: 'POST', url: `/api/files/presigned-url` as const }  
  return res
}

/**
 * @description Generate a presigned URL for direct upload to S3 (Admin and Doctor only). For images, category is REQUIRED (exercise, course, news, banner, profile, doctor). For videos, category is not needed.
 * @summary Generate presigned upload URL
 * {@link /api/files/presigned-url}
 */
export async function generatePresignedUploadUrl(data: GeneratePresignedUploadUrlMutationRequest, config: Partial<RequestConfig<GeneratePresignedUploadUrlMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<GeneratePresignedUploadUrlMutationResponse, ResponseErrorConfig<Error>, GeneratePresignedUploadUrlMutationRequest>({ method : "POST", url : getGeneratePresignedUploadUrlUrl().url.toString(), data : requestData, ... requestConfig })  
  return res.data
}