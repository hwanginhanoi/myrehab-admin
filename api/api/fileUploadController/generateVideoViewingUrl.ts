/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GenerateVideoViewingUrlQueryResponse, GenerateVideoViewingUrlQueryParams } from "../../types/GenerateVideoViewingUrl.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getGenerateVideoViewingUrlUrl() {
  const res = { method: 'GET', url: `/api/files/view/video` as const }  
  return res
}

/**
 * @description Generate a short-lived presigned URL for video streaming (strict access)
 * @summary Generate presigned video viewing URL
 * {@link /api/files/view/video}
 */
export async function generateVideoViewingUrl(params: GenerateVideoViewingUrlQueryParams, config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GenerateVideoViewingUrlQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGenerateVideoViewingUrlUrl().url.toString(), params, ... requestConfig })  
  return res.data
}