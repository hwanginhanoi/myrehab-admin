/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetCurrentCourseProgressQueryResponse } from "../../types/GetCurrentCourseProgress.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getGetCurrentCourseProgressUrl() {
  const res = { method: 'GET', url: `/api/course-progress/my-courses` as const }  
  return res
}

/**
 * @description Get the current course in progress for the user (can only have one)
 * @summary Get current course in progress
 * {@link /api/course-progress/my-courses}
 */
export async function getCurrentCourseProgress(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetCurrentCourseProgressQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGetCurrentCourseProgressUrl().url.toString(), ... requestConfig })  
  return res.data
}