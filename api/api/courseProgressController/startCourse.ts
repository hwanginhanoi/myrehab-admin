/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { StartCourseMutationRequest, StartCourseMutationResponse } from "../../types/StartCourse.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getStartCourseUrl() {
  const res = { method: 'POST', url: `/api/course-progress/start` as const }  
  return res
}

/**
 * @description User starts a purchased course
 * @summary Start a course
 * {@link /api/course-progress/start}
 */
export async function startCourse(data: StartCourseMutationRequest, config: Partial<RequestConfig<StartCourseMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<StartCourseMutationResponse, ResponseErrorConfig<Error>, StartCourseMutationRequest>({ method : "POST", url : getStartCourseUrl().url.toString(), data : requestData, ... requestConfig })  
  return res.data
}