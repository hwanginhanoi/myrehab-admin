/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetCurrentUserQueryResponse } from "../../types/GetCurrentUser.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getGetCurrentUserUrl() {
  const res = { method: 'GET', url: `/api/users/me` as const }  
  return res
}

/**
 * @description Returns current authenticated user's information including role and permissions
 * @summary Get current user info
 * {@link /api/users/me}
 */
export async function getCurrentUser(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetCurrentUserQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGetCurrentUserUrl().url.toString(), ... requestConfig })  
  return res.data
}