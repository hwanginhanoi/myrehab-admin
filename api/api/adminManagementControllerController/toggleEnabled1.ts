/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { ToggleEnabled1MutationResponse, ToggleEnabled1PathParams } from "../../types/ToggleEnabled1.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getToggleEnabled1Url(id: ToggleEnabled1PathParams["id"]) {
  const res = { method: 'PATCH', url: `/api/admins/${id}/toggle-enabled` as const }  
  return res
}

/**
 * {@link /api/admins/:id/toggle-enabled}
 */
export async function toggleEnabled1(id: ToggleEnabled1PathParams["id"], config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<ToggleEnabled1MutationResponse, ResponseErrorConfig<Error>, unknown>({ method : "PATCH", url : getToggleEnabled1Url(id).url.toString(), ... requestConfig })  
  return res.data
}