/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { UpdateRatingMutationRequest, UpdateRatingMutationResponse, UpdateRatingPathParams } from "../../types/UpdateRating.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getUpdateRatingUrl(courseId: UpdateRatingPathParams["courseId"]) {
  const res = { method: 'PUT', url: `/api/course-ratings/course/${courseId}` as const }  
  return res
}

/**
 * @description Update your existing rating for a course
 * @summary Update course rating
 * {@link /api/course-ratings/course/:courseId}
 */
export async function updateRating(courseId: UpdateRatingPathParams["courseId"], data: UpdateRatingMutationRequest, config: Partial<RequestConfig<UpdateRatingMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<UpdateRatingMutationResponse, ResponseErrorConfig<Error>, UpdateRatingMutationRequest>({ method : "PUT", url : getUpdateRatingUrl(courseId).url.toString(), data : requestData, ... requestConfig })  
  return res.data
}