/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { TogglePinMutationResponse, TogglePinPathParams } from "../../types/TogglePin.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getTogglePinUrl(id: TogglePinPathParams["id"]) {
  const res = { method: 'PATCH', url: `/api/news/${id}/toggle-pin` as const }  
  return res
}

/**
 * @description Pin or unpin a news article (Admin only)
 * @summary Toggle pin status
 * {@link /api/news/:id/toggle-pin}
 */
export async function togglePin(id: TogglePinPathParams["id"], config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<TogglePinMutationResponse, ResponseErrorConfig<Error>, unknown>({ method : "PATCH", url : getTogglePinUrl(id).url.toString(), ... requestConfig })  
  return res.data
}