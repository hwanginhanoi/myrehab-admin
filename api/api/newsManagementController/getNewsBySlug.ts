/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetNewsBySlugQueryResponse, GetNewsBySlugPathParams } from "../../types/GetNewsBySlug.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getGetNewsBySlugUrl(slug: GetNewsBySlugPathParams["slug"]) {
  const res = { method: 'GET', url: `/api/news/slug/${slug}` as const }  
  return res
}

/**
 * @description Retrieve a specific news article by its slug
 * @summary Get news by slug
 * {@link /api/news/slug/:slug}
 */
export async function getNewsBySlug(slug: GetNewsBySlugPathParams["slug"], config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetNewsBySlugQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGetNewsBySlugUrl(slug).url.toString(), ... requestConfig })  
  return res.data
}