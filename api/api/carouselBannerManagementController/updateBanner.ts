/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { UpdateBannerMutationRequest, UpdateBannerMutationResponse, UpdateBannerPathParams } from "../../types/UpdateBanner.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getUpdateBannerUrl(id: UpdateBannerPathParams["id"]) {
  const res = { method: 'PUT', url: `/api/carousel-banners/${id}` as const }  
  return res
}

/**
 * @description Update an existing carousel banner. All fields are required.
 * @summary Update banner (Admin only)
 * {@link /api/carousel-banners/:id}
 */
export async function updateBanner(id: UpdateBannerPathParams["id"], data: UpdateBannerMutationRequest, config: Partial<RequestConfig<UpdateBannerMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<UpdateBannerMutationResponse, ResponseErrorConfig<Error>, UpdateBannerMutationRequest>({ method : "PUT", url : getUpdateBannerUrl(id).url.toString(), data : requestData, ... requestConfig })  
  return res.data
}