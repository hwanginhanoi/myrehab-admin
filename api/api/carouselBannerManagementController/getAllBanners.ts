/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetAllBannersQueryResponse } from "../../types/GetAllBanners.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getGetAllBannersUrl() {
  const res = { method: 'GET', url: `/api/carousel-banners/admin` as const }  
  return res
}

/**
 * @description Retrieve all carousel banners including inactive ones. Returns full details including status information.
 * @summary Get all banners (Admin only)
 * {@link /api/carousel-banners/admin}
 */
export async function getAllBanners(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetAllBannersQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGetAllBannersUrl().url.toString(), ... requestConfig })  
  return res.data
}