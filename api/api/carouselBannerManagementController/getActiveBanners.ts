/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { GetActiveBannersQueryResponse } from "../../types/GetActiveBanners.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getGetActiveBannersUrl() {
  const res = { method: 'GET', url: `/api/carousel-banners` as const }  
  return res
}

/**
 * @description Public endpoint to retrieve all active banners that should be displayed. Banners are filtered by active status and scheduling dates. Ordered by displayOrder ascending.
 * @summary Get active banners for display
 * {@link /api/carousel-banners}
 */
export async function getActiveBanners(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetActiveBannersQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGetActiveBannersUrl().url.toString(), ... requestConfig })  
  return res.data
}