/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/api-client";
import type { ReorderBannersMutationRequest, ReorderBannersMutationResponse } from "../../types/ReorderBanners.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/api-client";

function getReorderBannersUrl() {
  const res = { method: 'PATCH', url: `/api/carousel-banners/reorder` as const }  
  return res
}

/**
 * @description Bulk reorder carousel banners. Provide banner IDs in desired display order. Position in the array becomes the displayOrder (0-indexed).
 * @summary Reorder banners (Admin only)
 * {@link /api/carousel-banners/reorder}
 */
export async function reorderBanners(data: ReorderBannersMutationRequest, config: Partial<RequestConfig<ReorderBannersMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<ReorderBannersMutationResponse, ResponseErrorConfig<Error>, ReorderBannersMutationRequest>({ method : "PATCH", url : getReorderBannersUrl().url.toString(), data : requestData, ... requestConfig })  
  return res.data
}